const primaryColor = "#5D4AC6";
const primaryColorRgba = "93,74,198";
const primaryColorHover = `rgba(${primaryColorRgba},.8)`;

const baseTextColor = "#393746";
const baseTextColorRgba = "57,55,70";

export const shadow = "2px 4px 4px rgba(0, 0, 0, 0.02);";
export const bgColors = { base: "#FAF9FD", primary: primaryColor };
export const textColors = {
  base: baseTextColor,
  gray: `rgba(${baseTextColorRgba}, .7)`,
  grayLight: `rgba(${baseTextColorRgba}, .4)`,
  primary: primaryColor,
  primaryHover: primaryColorHover,
};
export const borderColors = {
  base: "#efefef",
  input: "#ececec",
  primary: primaryColor,
};
